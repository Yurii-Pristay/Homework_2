export interface dbObject {
    readonly id: number,
    a?: Array<number>,
    b?: object,
    c?: number,
    d?: string,
    e?: boolean
};

export interface dObject {
  id: number,
  d: string,
  isPalindrome?: boolean
};